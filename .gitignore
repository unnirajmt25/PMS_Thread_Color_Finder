# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Excel lock files (created while a workbook is open)
~$*.xlsx

# python-thred-finder's CLI caches parsed workbook data alongside the
# source files.
Thread Chart/.thred_finder_cache.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Detailed internal project notes — kept local, not published to GitHub.
# The public-facing overview lives in README.md instead.
readme_project_details.md
